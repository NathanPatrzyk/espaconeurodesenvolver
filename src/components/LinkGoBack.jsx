const LinkGoBack = ({textColor}) => {
  return <a className={`text-${textColor} font-bold text-left underline inline-block mt-[70px] mb-2 md:mt-24 md:mb-8 md:mx-16 mx-4`} href="/">Voltar</a>;
};

export default LinkGoBack;
